import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  canvas: {
    height: 250,
    width: 300,
    right: -140,
    position: 'absolute',
    top: 20,
  },
});
export default styles;
